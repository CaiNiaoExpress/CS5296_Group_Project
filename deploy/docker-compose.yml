version: "3.9"
services:
  gateway:
    image: golang:1.22
    container_name: car-mall-gateway
    working_dir: /app
    volumes:
      - ../:/app
    command: sh -c "go run ./cmd/gateway"
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
